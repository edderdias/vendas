import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const App = () => {
    return (
        <SafeAreaView>
            <Text>Testando</Text>
        </SafeAreaView>
    )
};

export default App;