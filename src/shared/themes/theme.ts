import { buttonsTheme } from './buttons';
import { grayTheme, neutralTheme } from './colors';

export const theme = {
  colors: {
    grayTheme,
    neutralTheme,
  },
  buttons: {
    buttonsTheme,
  },
};
